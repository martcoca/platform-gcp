.terraform/
*.tfplan
*.tfstate
*.tfstate.*
config/local/
runtime/
*.plan.json

# Doctrine forbids committing these. Enforced by scripts/check-ignore-contract.sh
# after a saved plan carrying an OAuth client secret reached a public repository.
*.tfvars
